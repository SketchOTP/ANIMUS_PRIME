# Phase 8 qualification

Phase 8 makes progress explainable and operator-controlled:

- approved goal revisions are required before a baseline;
- baseline review is pending until explicitly approved;
- stable weighted goal items and structured evidence/status inputs are stored;
- percentages are reproducible from item completion and weights;
- confidence and freshness are separate fields;
- assessment history is retained per goal revision.
